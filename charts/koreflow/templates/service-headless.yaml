apiVersion: v1
kind: Service
metadata:
  name: koreflow-headless
spec:
  clusterIP: None
  selector:
    app: koreflow
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.nodePort }}
